services:
  - type: web
    name: freshsense-algorand-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: SENDER_MNEMONIC
        sync: false        # You will paste this manually in Render dashboard
